%div{:class=> type}
  .info
    =link_to image_tag("#{url}.jpg", :height => 59, :width => 49), "http://theyworkforyou.co.nz/mps/#{url}"
    -unless name.nil?
      =link_to name, "http://theyworkforyou.co.nz/mps/#{url}"
    - unless date.nil?
      .date=date.strftime('%d %b %Y') unless date.nil?
  .text=text
